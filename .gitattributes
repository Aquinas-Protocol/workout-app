# Normalize line endings to LF in the repository.
# The JS/TS/Expo toolchain expects LF, and this prevents the CRLF churn that
# core.autocrlf=true otherwise causes on Windows checkouts. Binary files are
# auto-detected by `text=auto` and left untouched.
* text=auto eol=lf
